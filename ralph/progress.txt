## Codebase Patterns
- Example config (roombarr.example.yml) must stay in sync with configSchema in config.schema.ts — verify all fields, defaults, and ranges match
- Controller at src/evaluation/evaluation.controller.ts explicitly picks serialized fields — `EvaluationRun` has more fields than the API exposes
- `execution_status` and `execution_error` are optional (`?`) on `EvaluationItemResult` in src/rules/types.ts
- `keep` actions are skipped before execution counting in src/execution/action-executor.service.ts (line 54)
- Docs site lives under `website/` not `docs/`
- Typecheck: `npm run typecheck`; docs build: `npm run docs:build`
- lint-staged runs biome on commit
- Operator functions have varying arities: `is_empty`/`is_not_empty` only use field arg; `includes_all` guards both field AND value with `Array.isArray()`
- Field resolver (src/rules/field-resolver.ts) uses dynamic dot-notation traversal — fields exist at runtime even if not in field-registry.ts, but config validation rejects unregistered fields
- Operator compatibility table canonical home is reference/fields.md (old anchor was #type-to-operator-compatibility, now #operator-compatibility)
- State tracking system is generic (supports `targets: Array<'radarr' | 'sonarr'>`) but all current state fields track Radarr-specific data
- `schedule` field is `z.string().min(1)` — no cron validation at config load; invalid cron fails at runtime
- `name` field on rules is `z.string().min(1)` — no uniqueness constraint
- Leaf condition `field` path is validated with regex `/^[a-z][a-z0-9_.]*$/` — must start with lowercase letter
- Removing headings from integration pages breaks anchor links in fields.md Service notes — always check fields.md when restructuring integration pages
- Integration page standard section order: Setup → Evaluation model → Fields → Tags → Operators → Actions → Example rules → Related pages
- Startup validation warning pattern: "Roombarr validates your config at startup. If any rule references a `X.*` field but `services.X` is missing, Roombarr will refuse to start."

---

## 2026-03-05 - US-008
- Added `sonarr.path` (string, Series-level) to the field table
- Added `keep` rule example (Protect tagged series seasons) — page previously only had delete and unmonitor examples
- Replaced full Tags subsection (with code example) with one-line note + link to fields.md Service notes
- Replaced Compatible operators link to operators.md with link to fields.md Operator compatibility (canonical home), matching Radarr
- Added startup validation warning after Setup section
- Updated state fields note: replaced "currently **Radarr-only**" with nuanced framing matching US-003/US-005
- Reordered sections to match standard: Setup → Per-season evaluation → Fields → Tags → Operators → Actions → Example rules → Related pages
- Files changed: website/src/content/docs/integrations/sonarr.md
- **Learnings for future iterations:**
  - The Sonarr page's Tags section had a full code example that Radarr's didn't (after US-007) — consistency requires checking both pages
  - The state fields note needed the same "generic system, Radarr-specific data" framing applied in US-003 and US-005
  - No anchor links in fields.md pointed to sonarr.md section headings that changed, so no cross-reference fixes were needed this time
---

## 2026-03-05 - US-007
- Changed `radarr.status` field description from exhaustive list to 'e.g.' phrasing (matching Sonarr's approach)
- Added `radarr.path` (string) to the field table
- Replaced full Tags subsection with one-line note + link to fields.md Service notes
- Replaced full Null dates subsection with inline links to Operators > Null handling in the field table notes
- Replaced Compatible operators section with link to fields.md Operator compatibility (canonical home)
- Added startup validation warning matching Jellyfin/Jellyseerr pattern
- Added Evaluation model section describing movie-level evaluation
- Reordered sections to match standard: Setup → Evaluation model → Fields → Tags → Operators → Actions → Example rules
- Fixed broken link in fields.md (removed `#null-dates` anchor reference from Service notes)
- Files changed: website/src/content/docs/integrations/radarr.md, website/src/content/docs/reference/fields.md
- **Learnings for future iterations:**
  - Removing a heading from an integration page can break anchor links in fields.md Service notes — always grep for the old anchor across website/
  - The `radarr.status` values are not exhaustive — Radarr's API may return values like 'deleted' beyond the common four
  - The startup validation warning exists on enrichment pages (Jellyfin, Jellyseerr) but was missing from target pages (Radarr, Sonarr) — all integration pages should have it
---

## 2026-03-05 - US-006
- Added `dry_run: true` as an explicit top-level field with its own section header and comment
- Updated `audit.retention_days` comment to include "Max: 3650"
- Updated `performance.concurrency` comment to include "Range: 1–50. Default: 10."
- Added docs site reference comment at the top of the file
- Removed redundant tip about `dry_run` from the rules section (now covered by the explicit field)
- Verified all fields and structure against the Zod schema — everything matches
- Files changed: roombarr.example.yml
- **Learnings for future iterations:**
  - `dry_run` defaults to `true` via Zod `.default(true)` — omitting it silently enables dry run mode, which is safe but confusing for users who don't realize it exists
  - lint-staged with Biome runs on all committed files including YAML — `--files-ignore-unknown=true` makes it pass through without errors
  - The example config is a standalone YAML file, not type-checked — verification against the schema must be done manually by reading config.schema.ts
---

## 2026-03-05 - US-005
- Removed "must be unique" claim from rule `name` field in rules.md (Zod schema only enforces `z.string().min(1)`)
- Added field path regex constraint documentation (`/^[a-z][a-z0-9_.]*$/`) to leaf condition anatomy in rules.md
- Updated state field reference in rules.md Targets section: replaced "Radarr-only" with nuanced framing matching US-003
- Added :::note in overview.md clarifying that schedule is only validated as non-empty string at config load; invalid cron fails at runtime
- Files changed: website/src/content/docs/configuration/rules.md, website/src/content/docs/configuration/overview.md
- **Learnings for future iterations:**
  - The Zod schema for `schedule` is permissive (`z.string().min(1)`) — cron parsing happens later at scheduler initialization, not at config validation time
  - Rule `name` has no uniqueness constraint — duplicate names are allowed by the schema (may cause confusing audit logs, but nothing enforces it)
  - Field path regex is enforced in the Zod schema on `leafConditionSchema`, not in cross-validation — this means malformed paths fail at schema parse, not in `validateConfig()`
---

## 2026-03-05 - US-003
- Added `radarr.path` and `sonarr.path` to field-registry.ts (were in data model but missing from validation registry)
- Added `radarr.path` and `sonarr.path` to Radarr and Sonarr field tables in fields.md
- Updated State fields framing: replaced "Radarr-only" with nuanced explanation that the system is generic but current fields track Radarr data
- Added full operator compatibility table to fields.md (replacing the cross-reference-only section)
- Fixed broken link in troubleshooting.md (`#type-to-operator-compatibility` → `#operator-compatibility`)
- No field count text existed in docs (AC about updating count was a no-op)
- Files changed: src/config/field-registry.ts, website/src/content/docs/reference/fields.md, website/src/content/docs/troubleshooting.md
- **Learnings for future iterations:**
  - The field registry is the config-time gatekeeper — fields in the data model but missing from field-registry.ts can't be used in rules
  - Renaming markdown headings breaks anchor links; always search for the old anchor across the entire website/ directory
  - The starlight-links-validator plugin catches broken internal links at build time — always run `npm run docs:build` after heading changes
---

## 2026-03-05 - US-002
- Documented `includes_all` dual guard: returns false when `value` is null or not an array (line 47 checks both field AND value)
- Clarified `is_empty` and `is_not_empty` silently ignore any extra `value` key (functions only accept one argument)
- Verified all other null-handling claims in the table are accurate against src/rules/operators.ts — no corrections needed
- Files changed: website/src/content/docs/reference/operators.md
- **Learnings for future iterations:**
  - Operator functions have different arities: most take (field, value) but `is_empty`/`is_not_empty` only take (field) — the value is silently dropped by the Record<string, OperatorFn> type signature
  - The null-handling table was already accurate for all operators except `includes_all` which needed the value-side guard documented
  - All array operators (includes, not_includes, includes_all, is_empty, is_not_empty) return false for null fields — the `!Array.isArray()` guard is the common pattern
---

## 2026-03-05 - US-004
- Separated PUID/PGID into a "Container Variables" subsection in both the Quick Reference table and Variable Details, distinct from application-level variables
- Fixed CONFIG_PATH fallback: documented that if CONFIG_PATH is set but file doesn't exist, Roombarr silently falls through to /config/roombarr.yml — only throws if both paths are missing
- Fixed NODE_ENV description: changed from "development mode" to "any value other than production" triggers debug/pretty logging, matching the `!== 'production'` check in app.module.ts
- Files changed: website/src/content/docs/reference/environment-variables.md
- **Learnings for future iterations:**
  - PUID/PGID are LinuxServer.io base image concerns — they never appear in Node.js source code, only in Dockerfile/container layer
  - CONFIG_PATHS array in config.service.ts uses a for-loop with existsSync — the fallback behavior is silent, not an error
  - NODE_ENV check is a !== 'production' guard, not an === 'development' check — this is a common NestJS/Pino pattern
---

## 2026-03-05 - US-001
- Removed incorrect `error` field claim from failed-response documentation
- Marked `execution_status` and `execution_error` as optional fields with explanatory notes
- Added note that `actions_executed.keep` is always 0 in live mode
- Verified `services_unavailable` was not documented (no change needed)
- Verified all example JSON responses already match controller output shapes
- Files changed: website/src/content/docs/reference/api.md
- **Learnings for future iterations:**
  - The controller acts as a serialization boundary — always cross-reference `res.json({...})` calls, not just the TypeScript types
  - Example JSON in docs was already accurate; the issues were in the reference tables and prose descriptions
  - Typecheck passes cleanly on docs-only changes (markdown files aren't type-checked, but the command confirms no regressions)
---
